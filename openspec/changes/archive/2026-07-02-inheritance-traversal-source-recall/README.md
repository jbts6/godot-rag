# inheritance-traversal-source-recall

Track production gap: Node inherits Object query doesn't recall Node class_summary into top-K, so C段 inherits traversal cannot fire. Feature works in unit test but is silent in production for its target query.
